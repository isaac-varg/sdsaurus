
const SectionTitle = ({title} : {title: string}) => {
    return (
        <h2 className="mb-2 px-4 text-lg text-stone-600 font-semibold tracking-tight">
            {title}
        </h2>
    )
}

export default SectionTitle